name: Lock closed inactive issues

on:
  schedule:
    # Run at 8:00 every day
    - cron: '0 8 * * *'

jobs:
  lock_closed:
    runs-on: ubuntu-latest
    steps:
      - uses: angular/dev-infra/github-actions/lock-closed@edafc47dd94893a32ba2399c72b09533f8fec5ea
        with:
          lock-bot-key: ${{ secrets.LOCK_BOT_PRIVATE_KEY }}
